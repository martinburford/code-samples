<!-- Components -->

import Bubble from "components/molecules/bubble";
import CodeBlock from ".storybook/components/code-block";
import Icon from "components/atoms/icon";
import InformationPanel from ".storybook/components/information-panel";
import StoryHeading from ".storybook/components/story-heading";

<!-- Storybook imports -->

import { Meta, Preview, Props, Story } from "@storybook/addon-docs";

<!-- Types -->

import { EColours, ESizes } from "types/enums";

<Meta
  parameters={{
    controls: {
      sort: "alpha",
    },
  }}
  title="Components/Molecules/Bubble"
/>

# Bubble

<InformationPanel leadIn="Bubble">
  This component is a generic bubble, allowing any JSX child content
</InformationPanel>

## Component props

<Props of={Bubble} />

<!-- Colours -->

<Preview withSource="none">
  <Story name="Colours">
    <>
      <StoryHeading
        heading="Colours"
        summary={
          <>
            To specify a background colour, pass the required colour to the{" "}
            <code>colour</code> prop. 2x colours from the applications colour
            palette are supported. By default, the background colour is set to
            be <code>primary-green</code> if not specified
          </>
        }
      />
      <Bubble colour={EColours.ERROR}>Error</Bubble>
      &nbsp;&nbsp;
      <Bubble colour={EColours.GRAY}>Gray</Bubble>
      &nbsp;&nbsp;
      <Bubble colour={EColours.PRIMARY}>Primary</Bubble>
      &nbsp;&nbsp;
      <Bubble colour={EColours.SUCCESS}>Success</Bubble>
      &nbsp;&nbsp;
      <Bubble colour={EColours.WARNING}>Warning</Bubble>
      <CodeBlock language="jsx">{`<Bubble colour="${EColours.ERROR}">Error</Bubble>
<Bubble colour="${EColours.GRAY}">Gray</Bubble>
<Bubble colour="${EColours.PRIMARY}">Primary</Bubble>
<Bubble colour="${EColours.SUCCESS}">Success</Bubble>
<Bubble colour="${EColours.WARNING}">Warning</Bubble>`}
      </CodeBlock>
    </>
  </Story>
</Preview>

<!-- With prefix icon -->

<Preview withSource="none">
  <Story name="With prefix icon">
    <>
      <StoryHeading
        heading="With prefix icon"
        summary={
          <>
            To have an icon show before the text of a bubble, provide an
            instance of an <code>&lt;Icon&gt;</code> component to the{" "}
            <code>prefixIcon</code> prop
          </>
        }
      />
      <Bubble colour={EColours.ERROR} prefixIcon={<Icon id="search" size={ESizes.XXS} />}>Error</Bubble>
      &nbsp;&nbsp;
      <Bubble colour={EColours.GRAY} prefixIcon={<Icon id="search" size={ESizes.XXS} />}>Gray</Bubble>
      &nbsp;&nbsp;
      <Bubble colour={EColours.PRIMARY} prefixIcon={<Icon id="search" size={ESizes.XXS} />}>Primary</Bubble>
      &nbsp;&nbsp;
      <Bubble colour={EColours.SUCCESS} prefixIcon={<Icon id="search" size={ESizes.XXS} />}>Success</Bubble>
      &nbsp;&nbsp;
      <Bubble colour={EColours.WARNING} prefixIcon={<Icon id="search" size={ESizes.XXS} />}>Warning</Bubble>
      <CodeBlock language="jsx">{`<Bubble colour="${EColours.ERROR}" prefixIcon={<Icon id="search" size="${ESizes.XXS}" />}>Error</Bubble>
<Bubble colour="${EColours.GRAY}" prefixIcon={<Icon id="search" size="${ESizes.XXS}" />}>Gray</Bubble>
<Bubble colour="${EColours.PRIMARY}" prefixIcon={<Icon id="search" size="${ESizes.XXS}" />}>Primary</Bubble>
<Bubble colour="${EColours.SUCCESS}" prefixIcon={<Icon id="search" size="${ESizes.XXS}" />}>Success</Bubble>
<Bubble colour="${EColours.WARNING}" prefixIcon={<Icon id="search" size="${ESizes.XXS}" />}>Warning</Bubble>`}
      </CodeBlock>
    </>
  </Story>
</Preview>

<!-- With suffix icon -->

<Preview withSource="none">
  <Story name="With suffix icon">
    <>
      <StoryHeading
        heading="With suffix icon"
        summary={
          <>
            To have an icon show after the text of a bubble, provide an instance
            of an <code>&lt;Icon&gt;</code> component to the{" "}
            <code>suffixIcon</code> prop
          </>
        }
      />
      <Bubble colour={EColours.ERROR} suffixIcon={<Icon id="exclamationInCircle" size={ESizes.XXS} />}>Error</Bubble>
      &nbsp;&nbsp;
      <Bubble colour={EColours.GRAY} suffixIcon={<Icon id="exclamationInCircle" size={ESizes.XXS} />}>Gray</Bubble>
      &nbsp;&nbsp;
      <Bubble colour={EColours.PRIMARY} suffixIcon={<Icon id="exclamationInCircle" size={ESizes.XXS} />}>Primary</Bubble>
      &nbsp;&nbsp;
      <Bubble colour={EColours.SUCCESS} suffixIcon={<Icon id="exclamationInCircle" size={ESizes.XXS} />}>Success</Bubble>
      &nbsp;&nbsp;
      <Bubble colour={EColours.WARNING} suffixIcon={<Icon id="exclamationInCircle" size={ESizes.XXS} />}>Warning</Bubble>
      <CodeBlock language="jsx">{`<Bubble colour="${EColours.ERROR}" suffixIcon={<Icon id="exclamationInCircle" size="${ESizes.XXS}" />}>Error</Bubble>
<Bubble colour="${EColours.GRAY}" suffixIcon={<Icon id="exclamationInCircle" size="${ESizes.XXS}" />}>Gray</Bubble>
<Bubble colour="${EColours.PRIMARY}" suffixIcon={<Icon id="exclamationInCircle" size="${ESizes.XXS}" />}>Primary</Bubble>
<Bubble colour="${EColours.SUCCESS}" suffixIcon={<Icon id="exclamationInCircle" size="${ESizes.XXS}" />}>Success</Bubble>
<Bubble colour="${EColours.WARNING}" suffixIcon={<Icon id="exclamationInCircle" size="${ESizes.XXS}" />}>Warning</Bubble>`}
      </CodeBlock>
    </>
  </Story>
</Preview>

<!-- With prefix and suffix icon -->

<Preview withSource="none">
  <Story name="With prefix and suffix icon">
    <>
      <StoryHeading
        heading="With prefix and suffix icon"
        summary={
          <>
            To have an icon show before the text of a bubble AND after the text
            of a bubble, provide an instance of an <code>&lt;Icon&gt;</code>{" "}
            component to both the
            <code>prefixIcon</code> and <code>suffixIcon</code> props
          </>
        }
      />
      <Bubble colour={EColours.ERROR} prefixIcon={<Icon id="search" size={ESizes.XXS} />} suffixIcon={<Icon id="exclamationInCircle" size={ESizes.XXS} />}>Error</Bubble>
      &nbsp;&nbsp;
      <Bubble colour={EColours.GRAY} prefixIcon={<Icon id="search" size={ESizes.XXS} />} suffixIcon={<Icon id="exclamationInCircle" size={ESizes.XXS} />}>Gray</Bubble>
      &nbsp;&nbsp;
      <Bubble colour={EColours.PRIMARY} prefixIcon={<Icon id="search" size={ESizes.XXS} />} suffixIcon={<Icon id="exclamationInCircle" size={ESizes.XXS} />}>Primary</Bubble>
      &nbsp;&nbsp;
      <Bubble colour={EColours.SUCCESS} prefixIcon={<Icon id="search" size={ESizes.XXS} />} suffixIcon={<Icon id="exclamationInCircle" size={ESizes.XXS} />}>Success</Bubble>
      &nbsp;&nbsp;
      <Bubble colour={EColours.WARNING} prefixIcon={<Icon id="search" size={ESizes.XXS} />} suffixIcon={<Icon id="exclamationInCircle" size={ESizes.XXS} />}>Warning</Bubble>
      <CodeBlock language="jsx">{`<Bubble colour="${EColours.ERROR}" prefixIcon={<Icon id="search" size="${ESizes.XXS}" />} suffixIcon={<Icon id="exclamationInCircle" size="${ESizes.XXS}" />}>Error</Bubble>
<Bubble colour="${EColours.GRAY}" prefixIcon={<Icon id="search" size="${ESizes.XXS}" />} suffixIcon={<Icon id="exclamationInCircle" size="${ESizes.XXS}" />}>Gray</Bubble>
<Bubble colour="${EColours.PRIMARY}" prefixIcon={<Icon id="search" size="${ESizes.XXS}" />} suffixIcon={<Icon id="exclamationInCircle" size="${ESizes.XXS}" />}>Primary</Bubble>
<Bubble colour="${EColours.SUCCESS}" prefixIcon={<Icon id="search" size="${ESizes.XXS}" />} suffixIcon={<Icon id="exclamationInCircle" size="${ESizes.XXS}" />}>Success</Bubble>
<Bubble colour="${EColours.WARNING}" prefixIcon={<Icon id="search" size="${ESizes.XXS}" />} suffixIcon={<Icon id="exclamationInCircle" size="${ESizes.XXS}" />}>Warning</Bubble>`}
      </CodeBlock>
    </>
  </Story>
</Preview>
